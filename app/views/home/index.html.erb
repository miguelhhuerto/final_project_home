<h1>Client Home Page</h1>

<% if user_signed_in? %>
  <h2> Hello, <%= current_user.username %> </h2>
  <%= link_to 'Sign out', destroy_user_session_path, data: { 'turbo-method': :delete } %>
<% else %>
  <%= link_to 'Sign in', new_user_session_path %>
<% end %>


<%= link_to 'Edit Profile', edit_user_registration_path if current_user%>
<%= link_to 'personal information', user_path if current_user %>
<%= link_to 'Address', user_addresses_path(@user) if current_user %>
<%= link_to 'Invite', user_invite_index_path if current_user %>
<%= link_to 'Lottery', lottery_index_path%>
<%= link_to 'shop', shop_index_path%>